# Basic Node App Notes

## Runtime

- Node 24 or newer.
- `npm test` is the default verification command.

## Coordination Notes

- Keep implementation claims scoped to `src` or `lib`.
- Use `tests` claims for verification-only work.
- Update this file when command setup, runtime assumptions, or deployment details change.
