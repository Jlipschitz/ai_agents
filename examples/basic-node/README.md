# Basic Node Example

This example is for a small Node service or library with source files, tests, and documentation.

Useful first commands:

```bash
npm run agents:doctor
npm run agents -- start agent-1 task-api --paths src "Implement API behavior"
npm run agents -- run-check test
```

The config favors two agents: one on implementation, one on verification or docs.
